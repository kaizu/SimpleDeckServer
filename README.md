# Simple Environment Server

## How to Run

### Preparation

```sh
pip install -r requirements.txt
```

### Run

```sh
uvicorn main:app --reload
```

The APIs can be available at <localhost:8000/docs>.